
git --version

git config --global user.name "***"
git config --global user.email "***@***"
git config --global color.ui auto

ローカルリポジトリの作成
git init (dirname)

ステータスの確認
git status

ステージング
git add filename(*)